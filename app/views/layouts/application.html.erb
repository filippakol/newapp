<!DOCTYPE html>
<html>
  <head>
    <title>Stretch Master</title>
    <%= csrf_meta_tags %>

    <!-- Expand the number of characters we can use in the document beyond basic ASCII 🎉 -->
    <meta charset="utf-8">

    <!-- Make it responsive to small screens -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all' %>

    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.css">

  </head>

  <body>
  <nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/body_parts"> Stretch Master <a>
    </div>
 <ul class="nav navbar-nav">
      <li><a href="/stretches">List of all stretches</a></li>
     <% if session[:user_id]== nil %>
      <li> <a href="/user_sign_in">Sign in</a></li>
      <li> <a href="/user_sign_up">Sign Up</a></li>
      <% else %>
      <li> <a href="/bookmarks">My Bookmarks</a></li>
      <li> <a href="/user_sign_out">Sign Out</a></li>
    <% end %>
    </ul>
  </div>
</nav>
  <p style="color: green"><%=notice%></p>
   <p style="color: red"><%=alert%></p>
    <%= yield %>
  </body>
</html>
